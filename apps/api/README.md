# Bagheera API

NestJS backend. Everything runs in Docker — see the repository root `README.md` and `CLAUDE.md` for setup, commands and architecture.

- Swagger UI: http://localhost:3000/api/docs (with `make up`)
- Unit tests: `make test-unit`; integration tests (Testcontainers): `make test-integration`
