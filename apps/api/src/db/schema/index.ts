// Table definitions land here as business schema steps (Phase B) are implemented.
// Kept as an explicit empty barrel so drizzle-kit has a valid schema entrypoint
// before any tables exist.
export {};
