{% extends 'KrevindiouBagheeraBundle::base.html.twig' %}

{% block page_title 'member.forgot_password.page_title'|trans %}

{% block breadcrumb %}
    <li class="active">{{ 'member.forgot_password.page_title'|trans }}</li>
{% endblock %}

{% block content %}
<form action="{{ path('member_forgot_password') }}" novalidate="novalidate" method="post" class="form-horizontal" {{ form_enctype(forgotPasswordForm) }}>
    {{ form_widget(forgotPasswordForm) }}

    <div class="form-actions">
        <input type="submit" class="btn btn-primary" value="{{ 'member.forgot_password.submit_button'|trans }}"/>
        <a href="{{ path('member_login') }}" class="btn">{{ 'cancel_link'|trans }}</a>
    </div>
</form>
{% endblock %}
