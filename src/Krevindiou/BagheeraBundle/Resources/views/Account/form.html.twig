{% extends 'KrevindiouBagheeraBundle::base.html.twig' %}

{% set account = accountForm.vars.value %}

{% block page_title account.accountId is null ? 'account.new_form_page_title'|trans : 'account.edit_form_page_title'|trans %}

{% block breadcrumb %}
    <li class="active">{{ account.accountId is null ? 'account.new_form_page_title'|trans : 'account.edit_form_page_title'|trans }}</li>
{% endblock %}

{% block content %}
<form action="" novalidate="novalidate" method="post" class="form-horizontal" {{ form_enctype(accountForm) }}>
    {{ form_row(accountForm.bank) }}
    {{ form_row(accountForm.name) }}
    {{ form_row(accountForm.currency) }}

    {% if accountForm.initialBalance is defined %}
    {{ form_row(accountForm.initialBalance) }}
    {% endif %}

    {{ form_row(accountForm.overdraftFacility) }}
    {{ form_rest(accountForm) }}

    <div class="form-actions">
        <input type="submit" class="btn btn-primary" value="{{ 'account.form_submit_button'|trans }}"/>
        <a href="{{ path('account_list') }}" class="btn">{{ 'cancel_link'|trans }}</a>
    </div>
</form>
{% endblock %}
