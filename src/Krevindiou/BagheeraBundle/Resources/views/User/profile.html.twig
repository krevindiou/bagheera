{% extends '::base.html.twig' %}

{% block content %}
<h1>{% trans %}user_profile_page_title{% endtrans %}</h1>

<form action="{{ path('user_profile') }}" novalidate="novalidate" method="post" {{ form_enctype(profileForm) }}>
    {{ form_widget(profileForm) }}

    <div class="formButton">
        <input type="submit" value="{% trans %}user_profile_submit_button{% endtrans %}"/>
    </div>
</form>
{% endblock %}
