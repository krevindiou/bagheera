{% extends '::base.html.twig' %}

{% block content %}
<h1>Reset password</h1>

<form action="{{ path('user_reset_password', {'key': key}) }}" novalidate="novalidate" method="post" {{ form_enctype(resetPasswordForm) }}>
    {{ form_widget(resetPasswordForm) }}

    <div class="formButton">
        <input type="submit" value="Reset password"/>
    </div>
</form>
{% endblock %}
