{% extends '::base.html.twig' %}

{% block pageTitle 'bank_access_form_page_title'|trans %}

{% block content %}
<form action="" novalidate="novalidate" method="post" class="form-horizontal" {{ form_enctype(bankAccessForm) }}>
    {{ form_widget(bankAccessForm) }}

    <div class="control-group">
        <div class="controls">
            <input type="submit" class="btn btn-large btn-primary" value="{{ 'bank_access_form_submit_button'|trans }}"/>
        </div>
    </div>
</form>
{% endblock %}
