#!/usr/bin/env bash

cid=$(docker ps -a|grep php|cut -c 1-12)

project_dir=$(cd "$(dirname "$0")" && cd .. && pwd)
project_name=$(basename "$project_dir")

docker-compose up -d

docker exec -ti $cid "$project_dir"/bin/build $project_dir

"$project_dir"/bin/http

docker-compose logs
