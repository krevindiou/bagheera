#!/usr/bin/env bash

project_dir=$(cd "$(dirname "$0")" && cd .. && pwd)
env=$1

suffix='dev'
if [[ $env = 'prod' ]]; then
    suffix='prod'
fi

docker-compose -f "$project_dir/docker/docker-compose-$suffix.yml" logs -f "${@:2}"
