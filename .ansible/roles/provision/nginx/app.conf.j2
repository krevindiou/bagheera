server {
    listen 80;
    listen [::]:80;

    server_name {{ domain if domain is defined else '_' }};

    location / {
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass http://localhost:8080;
    }
}
